{
  "name": "dreamwalker-gallery",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --watch",
    "build": "eleventy",
    "clean": "rimraf _site .cache",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "rimraf": "^5.0.0",
    "slugify": "^1.6.6",
    "wrangler": "^4.129.0"
  }
}
