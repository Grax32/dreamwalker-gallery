{
  "name": "dreamwalker-gallery",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "clean": "rimraf _site .cache"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-img": "^1.3.0",
    "slugify": "^1.6.6",
    "rimraf": "^5.0.0"
  }
}
